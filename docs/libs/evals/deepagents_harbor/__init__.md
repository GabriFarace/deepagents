# `evals/deepagents_harbor/__init__.py`

> Harbor integration with LangChain Deep Agents and LangSmith tracing.

## Position in the system

This module belongs to the Harbor integration layer. It adapts Harbor environments and LangSmith metadata into interfaces that the deepagents SDK and eval tooling can consume.

## Imports and module-level state

This file imports `deepagents_harbor.backend, deepagents_harbor.deepagents_wrapper, deepagents_harbor.failure, deepagents_harbor.langsmith, deepagents_harbor.langsmith_environment, deepagents_harbor.metadata`.

## Functions and classes

This module has no public functions or classes. It is intentionally limited to package markers, version metadata, or typing markers.

## Gotchas

This file is intentionally small; keep documentation concise and check version sync before release changes.
