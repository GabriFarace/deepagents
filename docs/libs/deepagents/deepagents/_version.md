# `libs/deepagents/deepagents/_version.py`

> SDK version constant.

## Position in the system

The package root imports `__version__` from this file and re-exports it as
`deepagents.__version__`.

## Imports and module-level state

`__version__ = "0.5.8"` is the only value in the file. The
`x-release-please-version` comment marks it for release automation.

## Functions and classes

This file defines no functions or classes. It is intentionally concise and is
covered by the package README as a root helper.

## Gotchas

When release tooling updates package versions, this value must stay in sync
with the SDK package metadata.

